<h1>Liste de tous les utilisateurs</h1>
<ul>
  <% @users.each do |user| %>
    <li>
      <%= link_to "#{user.first_name} #{user.last_name}", user_path(user) %> (ID: <%= user.id %>)
    </li>
  <% end %>
</ul>